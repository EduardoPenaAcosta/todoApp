import React from 'react';

import {
    HeaderView,
    HeaderTitle
} from '../constants/appStyles'

const Header = () => {

    return(
        <HeaderView>
            <HeaderTitle>todoListApp</HeaderTitle>
        </HeaderView>
    );
}

export default Header;